---
export interface Props {
  title?: string;
}

const { title } = Astro.props;
const { isLoggedIn } = Astro.locals;
---

<!DOCTYPE html>
<html lang="en" data-theme="night">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{`${title ? `${title} | ` : ''}Mój samochód`}</title>
  </head>
  <body class="bg-base-200">
    <div class="mx-auto h-screen w-screen max-w-screen-sm">
      <header class="navbar flex justify-between bg-base-100 p-4">
        <h1 class="text-xl font-bold">Mój samochód</h1>
        {
          isLoggedIn && (
            <a href="/logout" class="btn-secondary btn-xs btn">
              Wyloguj
            </a>
          )
        }
      </header>
      <main class="mt-4 flex flex-col items-center justify-center">
        <slot />
      </main>
    </div>
  </body>
</html>
